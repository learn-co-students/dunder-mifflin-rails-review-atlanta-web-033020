<h1> <%= @dog.name %> </h1>

<h4>Breed: <%= @dog.breed %> </h4>
<h4>Age: <%= @dog.age %> </h4>

<h4>Employees:</h4>
<ul>
  <% @dog.employees.each do |employee| %>
    <li>
      <%= link_to employee.name, employee_path(employee) %>
    </li>
  <% end %>
</ul>